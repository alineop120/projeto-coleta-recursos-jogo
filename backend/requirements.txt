# Flask core
Flask==2.3.3

# CORS support (útil para conectar o frontend React com o backend Flask)
Flask-Cors==4.0.0

# Caso você use Flask com ambiente .env
python-dotenv==1.0.1

# Dependência opcional para requisições HTTP (caso seu backend se comunique com APIs externas)
requests==2.31.0